//// FormConfigVariable Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db
import glimr/http/response.{type Response}

pub type FormConfigVariable {
  FormConfigVariable(
    form_config_id: Int,
    yearly_fee: Float,
    variable_costs: Float,
  )
}

fn row_decoder() -> decode.Decoder(FormConfigVariable) {
  use form_config_id <- decode.field(0, decode.int)
  use yearly_fee <- decode.field(1, decode.float)
  use variable_costs <- decode.field(2, decode.float)
  decode.success(FormConfigVariable(form_config_id, yearly_fee, variable_costs))
}

pub fn encoder() -> fn(FormConfigVariable) -> json.Json {
  fn(model: FormConfigVariable) {
    json.object([
      #("form_config_id", json.int(model.form_config_id)),
      #("yearly_fee", json.float(model.yearly_fee)),
      #("variable_costs", json.float(model.variable_costs)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(FormConfigVariable) {
  use form_config_id <- decode.field("form_config_id", decode.int)
  use yearly_fee <- decode.field("yearly_fee", decode.float)
  use variable_costs <- decode.field("variable_costs", decode.float)
  decode.success(FormConfigVariable(form_config_id, yearly_fee, variable_costs))
}

pub fn create(
  pool pool: db.DbPool,
  form_config_id form_config_id: Int,
  yearly_fee yearly_fee: Float,
  variable_costs variable_costs: Float,
) -> Result(Int, db.DbError) {
  use connection <- db.get_connection(pool)
  create_wc(
    connection: connection,
    form_config_id: form_config_id,
    yearly_fee: yearly_fee,
    variable_costs: variable_costs,
  )
}

pub fn create_wc(
  connection connection: db.Connection,
  form_config_id form_config_id: Int,
  yearly_fee yearly_fee: Float,
  variable_costs variable_costs: Float,
) -> Result(Int, db.DbError) {
  db.exec_with(
    connection,
    "INSERT INTO form_configs_variable (form_config_id, yearly_fee, variable_costs) VALUES ($1, $2, $3)",
    [db.int(form_config_id), db.float(yearly_fee), db.float(variable_costs)],
  )
}

pub fn create_or_fail(
  pool pool: db.DbPool,
  form_config_id form_config_id: Int,
  yearly_fee yearly_fee: Float,
  variable_costs variable_costs: Float,
  then then: fn(Int) -> Response,
) -> Response {
  use connection <- db.get_connection(pool)
  create_or_fail_wc(
    connection: connection,
    form_config_id: form_config_id,
    yearly_fee: yearly_fee,
    variable_costs: variable_costs,
    then: then,
  )
}

pub fn create_or_fail_wc(
  connection connection: db.Connection,
  form_config_id form_config_id: Int,
  yearly_fee yearly_fee: Float,
  variable_costs variable_costs: Float,
  then then: fn(Int) -> Response,
) -> Response {
  case
    create_wc(
      connection: connection,
      form_config_id: form_config_id,
      yearly_fee: yearly_fee,
      variable_costs: variable_costs,
    )
  {
    Ok(count) -> then(count)
    Error(db.ConnectionError(_)) -> response.empty(503)
    Error(db.TimeoutError) -> response.empty(503)
    Error(_) -> response.internal_server_error()
  }
}
