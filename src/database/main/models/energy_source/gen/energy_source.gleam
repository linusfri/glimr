//// EnergySource Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db

pub type EnergySource {
  EnergySource(
    id: Int,
    agreement_id: Int,
    label: String,
    value: String,
    price: Float,
  )
}

fn row_decoder() -> decode.Decoder(EnergySource) {
  use id <- decode.field(0, decode.int)
  use agreement_id <- decode.field(1, decode.int)
  use label <- decode.field(2, decode.string)
  use value <- decode.field(3, decode.string)
  use price <- decode.field(4, decode.float)
  decode.success(EnergySource(id, agreement_id, label, value, price))
}

pub fn encoder() -> fn(EnergySource) -> json.Json {
  fn(model: EnergySource) {
    json.object([
      #("id", json.int(model.id)),
      #("agreement_id", json.int(model.agreement_id)),
      #("label", json.string(model.label)),
      #("value", json.string(model.value)),
      #("price", json.float(model.price)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(EnergySource) {
  use id <- decode.field("id", decode.int)
  use agreement_id <- decode.field("agreement_id", decode.int)
  use label <- decode.field("label", decode.string)
  use value <- decode.field("value", decode.string)
  use price <- decode.field("price", decode.float)
  decode.success(EnergySource(id, agreement_id, label, value, price))
}
