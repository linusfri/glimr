//// SubmissionPriceVariable Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db

pub type SubmissionPriceVariable {
  SubmissionPriceVariable(submission_price_id: Int, price_variable: Float)
}

fn row_decoder() -> decode.Decoder(SubmissionPriceVariable) {
  use submission_price_id <- decode.field(0, decode.int)
  use price_variable <- decode.field(1, decode.float)
  decode.success(SubmissionPriceVariable(submission_price_id, price_variable))
}

pub fn encoder() -> fn(SubmissionPriceVariable) -> json.Json {
  fn(model: SubmissionPriceVariable) {
    json.object([
      #("submission_price_id", json.int(model.submission_price_id)),
      #("price_variable", json.float(model.price_variable)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(SubmissionPriceVariable) {
  use submission_price_id <- decode.field("submission_price_id", decode.int)
  use price_variable <- decode.field("price_variable", decode.float)
  decode.success(SubmissionPriceVariable(submission_price_id, price_variable))
}
