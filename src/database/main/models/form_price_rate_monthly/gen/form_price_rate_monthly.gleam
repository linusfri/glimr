//// FormPriceRateMonthly Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db

pub type FormPriceRateMonthly {
  FormPriceRateMonthly(
    form_price_rate_id: Int,
    rate: Float,
    valid_month: String,
  )
}

fn row_decoder() -> decode.Decoder(FormPriceRateMonthly) {
  use form_price_rate_id <- decode.field(0, decode.int)
  use rate <- decode.field(1, decode.float)
  use valid_month <- decode.field(2, decode.string)
  decode.success(FormPriceRateMonthly(form_price_rate_id, rate, valid_month))
}

pub fn encoder() -> fn(FormPriceRateMonthly) -> json.Json {
  fn(model: FormPriceRateMonthly) {
    json.object([
      #("form_price_rate_id", json.int(model.form_price_rate_id)),
      #("rate", json.float(model.rate)),
      #("valid_month", json.string(model.valid_month)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(FormPriceRateMonthly) {
  use form_price_rate_id <- decode.field("form_price_rate_id", decode.int)
  use rate <- decode.field("rate", decode.float)
  use valid_month <- decode.field("valid_month", decode.string)
  decode.success(FormPriceRateMonthly(form_price_rate_id, rate, valid_month))
}
