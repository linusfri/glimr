//// GroupMix Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db

pub type GroupMix {
  GroupMix(group_id: Int, fixed_percent: Float)
}

fn row_decoder() -> decode.Decoder(GroupMix) {
  use group_id <- decode.field(0, decode.int)
  use fixed_percent <- decode.field(1, decode.float)
  decode.success(GroupMix(group_id, fixed_percent))
}

pub fn encoder() -> fn(GroupMix) -> json.Json {
  fn(model: GroupMix) {
    json.object([
      #("group_id", json.int(model.group_id)),
      #("fixed_percent", json.float(model.fixed_percent)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(GroupMix) {
  use group_id <- decode.field("group_id", decode.int)
  use fixed_percent <- decode.field("fixed_percent", decode.float)
  decode.success(GroupMix(group_id, fixed_percent))
}
