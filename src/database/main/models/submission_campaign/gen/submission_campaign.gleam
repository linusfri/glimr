//// SubmissionCampaign Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import gleam/option.{type Option}
import glimr/db/db

pub type SubmissionCampaign {
  SubmissionCampaign(
    submission_id: Int,
    campaign_id: String,
    offer_type: Option(String),
  )
}

fn row_decoder() -> decode.Decoder(SubmissionCampaign) {
  use submission_id <- decode.field(0, decode.int)
  use campaign_id <- decode.field(1, decode.string)
  use offer_type <- decode.field(2, decode.optional(decode.string))
  decode.success(SubmissionCampaign(submission_id, campaign_id, offer_type))
}

pub fn encoder() -> fn(SubmissionCampaign) -> json.Json {
  fn(model: SubmissionCampaign) {
    json.object([
      #("submission_id", json.int(model.submission_id)),
      #("campaign_id", json.string(model.campaign_id)),
      #("offer_type", json.nullable(model.offer_type, json.string)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(SubmissionCampaign) {
  use submission_id <- decode.field("submission_id", decode.int)
  use campaign_id <- decode.field("campaign_id", decode.string)
  use offer_type <- decode.field("offer_type", decode.optional(decode.string))
  decode.success(SubmissionCampaign(submission_id, campaign_id, offer_type))
}
