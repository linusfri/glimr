//// FormConfigSpot Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db

pub type FormConfigSpot {
  FormConfigSpot(
    form_config_id: Int,
    yearly_fee: Float,
    variable_costs: Float,
    surcharge: Float,
  )
}

fn row_decoder() -> decode.Decoder(FormConfigSpot) {
  use form_config_id <- decode.field(0, decode.int)
  use yearly_fee <- decode.field(1, decode.float)
  use variable_costs <- decode.field(2, decode.float)
  use surcharge <- decode.field(3, decode.float)
  decode.success(FormConfigSpot(
    form_config_id,
    yearly_fee,
    variable_costs,
    surcharge,
  ))
}

pub fn encoder() -> fn(FormConfigSpot) -> json.Json {
  fn(model: FormConfigSpot) {
    json.object([
      #("form_config_id", json.int(model.form_config_id)),
      #("yearly_fee", json.float(model.yearly_fee)),
      #("variable_costs", json.float(model.variable_costs)),
      #("surcharge", json.float(model.surcharge)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(FormConfigSpot) {
  use form_config_id <- decode.field("form_config_id", decode.int)
  use yearly_fee <- decode.field("yearly_fee", decode.float)
  use variable_costs <- decode.field("variable_costs", decode.float)
  use surcharge <- decode.field("surcharge", decode.float)
  decode.success(FormConfigSpot(
    form_config_id,
    yearly_fee,
    variable_costs,
    surcharge,
  ))
}
