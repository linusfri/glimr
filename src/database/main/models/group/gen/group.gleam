//// Group Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import gleam/result
import glimr/db/db

pub type GroupType {
  Fixed
  Mix
}

pub fn group_type_to_string(value: GroupType) -> String {
  case value {
    Fixed -> "fixed"
    Mix -> "mix"
  }
}

pub fn group_type_from_string(value: String) -> Result(GroupType, Nil) {
  case value {
    "fixed" -> Ok(Fixed)
    "mix" -> Ok(Mix)
    _ -> Error(Nil)
  }
}

pub type Group {
  Group(
    id: Int,
    form_config_id: Int,
    group_type: GroupType,
    title: String,
    contract_period_months: Int,
    yearly_fee: Float,
    visible_from: String,
    visible_until: String,
  )
}

fn row_decoder() -> decode.Decoder(Group) {
  use id <- decode.field(0, decode.int)
  use form_config_id <- decode.field(1, decode.int)
  use group_type <- decode.field(2, decode.string)
  use title <- decode.field(3, decode.string)
  use contract_period_months <- decode.field(4, decode.int)
  use yearly_fee <- decode.field(5, decode.float)
  use visible_from <- decode.field(6, decode.string)
  use visible_until <- decode.field(7, decode.string)
  decode.success(Group(
    id,
    form_config_id,
    result.unwrap(group_type_from_string(group_type), Fixed),
    title,
    contract_period_months,
    yearly_fee,
    visible_from,
    visible_until,
  ))
}

pub fn encoder() -> fn(Group) -> json.Json {
  fn(model: Group) {
    json.object([
      #("id", json.int(model.id)),
      #("form_config_id", json.int(model.form_config_id)),
      #("group_type", json.string(group_type_to_string(model.group_type))),
      #("title", json.string(model.title)),
      #("contract_period_months", json.int(model.contract_period_months)),
      #("yearly_fee", json.float(model.yearly_fee)),
      #("visible_from", json.string(model.visible_from)),
      #("visible_until", json.string(model.visible_until)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(Group) {
  use id <- decode.field("id", decode.int)
  use form_config_id <- decode.field("form_config_id", decode.int)
  use group_type <- decode.field("group_type", decode.string)
  use title <- decode.field("title", decode.string)
  use contract_period_months <- decode.field(
    "contract_period_months",
    decode.int,
  )
  use yearly_fee <- decode.field("yearly_fee", decode.float)
  use visible_from <- decode.field("visible_from", decode.string)
  use visible_until <- decode.field("visible_until", decode.string)
  decode.success(Group(
    id,
    form_config_id,
    result.unwrap(group_type_from_string(group_type), Fixed),
    title,
    contract_period_months,
    yearly_fee,
    visible_from,
    visible_until,
  ))
}
