//// SubmissionPriceFixed Model (GENERATED - DO NOT EDIT)
////
//// Generated by Glimr ✨

import gleam/dynamic/decode
import gleam/json
import glimr/db/db

pub type SubmissionPriceFixed {
  SubmissionPriceFixed(submission_price_id: Int, price: Float)
}

fn row_decoder() -> decode.Decoder(SubmissionPriceFixed) {
  use submission_price_id <- decode.field(0, decode.int)
  use price <- decode.field(1, decode.float)
  decode.success(SubmissionPriceFixed(submission_price_id, price))
}

pub fn encoder() -> fn(SubmissionPriceFixed) -> json.Json {
  fn(model: SubmissionPriceFixed) {
    json.object([
      #("submission_price_id", json.int(model.submission_price_id)),
      #("price", json.float(model.price)),
    ])
  }
}

pub fn decoder() -> decode.Decoder(SubmissionPriceFixed) {
  use submission_price_id <- decode.field("submission_price_id", decode.int)
  use price <- decode.field("price", decode.float)
  decode.success(SubmissionPriceFixed(submission_price_id, price))
}
